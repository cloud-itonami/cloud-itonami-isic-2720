name: regenerate-board
on:
  schedule:
    - cron: '37 20 * * *'
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { path: orgs/cloud-itonami/cloud-itonami-isic-2720 }
      - uses: actions/checkout@v4
        with: {repository: kotoba-lang/langgraph, path: orgs/kotoba-lang/langgraph}
      - uses: actions/checkout@v4
        with: {repository: kotoba-lang/langchain, path: orgs/kotoba-lang/langchain}
      - uses: actions/checkout@v4
        with: {repository: kotoba-lang/robotics, path: orgs/kotoba-lang/robotics}
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: "21" }
      - uses: DeLaGuardo/setup-clojure@13.0
        with: { cli: latest }
      - name: regenerate
        working-directory: orgs/cloud-itonami/cloud-itonami-isic-2720
        run: clojure -M:dev:render-html
      - name: verify
        working-directory: orgs/cloud-itonami/cloud-itonami-isic-2720
        run: |
          grep -q 'cellworks' docs/samples/operator-console.html
          test "$(wc -c < docs/samples/operator-console.html)" -gt 1000
          echo "ok"
      - name: commit when changed
        working-directory: orgs/cloud-itonami/cloud-itonami-isic-2720
        run: |
          git add docs/
          if ! git diff --cached --quiet; then
            git config user.name "regenerate-board[bot]"
            git config user.email "noreply@github.com"
            git commit -m "chore: nightly regen"
            git push
          else
            echo "unchanged"
          fi
